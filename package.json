{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm run --parallel build",
    "preview": "pnpm run --parallel preview",
    "check": "pnpm run --parallel typecheck && biome check",
    "test": "pnpm run --parallel test",
    "clear": "pnpm run --parallel clear",
    "clean": "pnpm run --parallel clean && node scripts/clean.js"
  },
  "engines": {
    "pnpm": ">=11.0.3"
  },
  "dependencies": {
    "@biomejs/biome": "2.4.14"
  }
}
