{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --parallel build",
    "preview": "pnpm --parallel preview",
    "check": "pnpm --parallel typecheck && biome check",
    "test": "pnpm --parallel test",
    "clear": "pnpm --parallel clear",
    "clean": "pnpm --parallel clean && node scripts/clean.js"
  },
  "engines": {
    "pnpm": ">=10.20.0"
  },
  "dependencies": {
    "@biomejs/biome": "2.3.11"
  }
}
